rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // قواعد مبسطة للاختبار
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
